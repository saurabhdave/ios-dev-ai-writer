You are a principal iOS architect and technical writer.

Topic:
"{topic}"

Create a concise markdown outline for a Medium article.

Requirements:
- Keep a clear Medium-style reading flow with short, scannable sections.
- Use this section order exactly:
  1) opening hook,
  2) why this topic matters for iOS teams right now,
  3) 4-5 core sections,
  4) tradeoffs/pitfalls,
  5) implementation checklist,
  6) closing takeaway.
- Use `##` for major sections and `###` for sub-points.
- Format core sections as numbered headings: `## 1. ...`, `## 2. ...`.
- For each core section, add 2-3 practical sub-points that include:
  - one concrete Apple API/tool/framework callout,
  - one "when to use vs when not to use" decision point,
  - one production caution (failure mode, rollout, or operational risk).
- Include at least one core section focused on testing/observability (XCTest, Instruments, MetricKit, os_log/signposts, crash monitoring, etc.).
- Prefer real team concerns: migration strategy, backward compatibility, release risk, incident handling, and maintainability.
- Keep the outline compact and directly useful for a 900-1200 word article.

You are a principal iOS engineer writing a professional Medium article.

Topic:
"{topic}"

Outline:
{outline}

Verified source links (may be referenced only in a final references section outside this body):
{allowed_references}

Task:
Write the article body in markdown (NO top-level # title).

Hard requirements:
- Target length: 900-1200 words.
- Audience: intermediate to senior iOS developers.
- Tone: professional, practical, and precise.
- Use clear ## and ### headings, following the outline order.
- Use title case for section headings (e.g., `## Why This Matters For iOS Teams`, NOT `## WHY THIS MATTERS`). Never use all-uppercase headings.
- Format core sections as numbered `##` headings (for example: `## 1. ...`, `## 2. ...`).
- Keep paragraphs short and readable (prefer 2-4 sentences each).
- Start with a concise hook intro before the first `##` heading.
- Add one context-setting section early ("why this matters" or equivalent).
- Keep 4-5 core sections as scannable chunks with practical subheadings.
- Include at least one `###` subheading inside each core section.
- Include at least one concise markdown list (`-` or `1.`) where it improves scannability.
- Include one short pull-quote style insight using markdown blockquote (`> ...`).
- Keep heading hierarchy clean (`##` and `###` only; no deeper heading levels).
- Include one section explicitly focused on tradeoffs/pitfalls.
- Include engineering tradeoffs and concrete implementation advice.
- Include one practical checklist section.
- End with a short closing takeaway section.
- Write from a real-world Apple software engineering perspective, not a tutorial tone.
- In every core section, include:
  - one concrete Apple API/tool/framework name,
  - one architecture decision criterion ("choose X when..., choose Y when..."),
  - one production concern (testing, observability, rollout, migration, or failure handling).
- Always format Swift API names, types, methods, and properties using inline backtick code: write `withCheckedThrowingContinuation`, NOT withCheckedThrowingContinuation. Write `@Observable`, NOT @Observable. Write `URLSession.data(for:)`, NOT URLSession.data(for:). Every API name mentioned in prose MUST use backtick formatting.
- Include at least one section that covers validation/operations (XCTest async expectations, Instruments Time Profiler/Allocations template, os_signpost for marking async boundaries, MetricKit for post-release signals, os_log structured logging, rollout gates).
- For any Swift language feature or framework API introduced after iOS 15 / Swift 5.5, state the minimum Swift version or iOS deployment target in the relevant section (e.g., "requires iOS 17+ / Swift 5.9").
- Prefer specific implementation details over abstract principles.
- Avoid generic phrases like "best practices", "in today's fast-paced world", "it is important to", "leverage", or "game-changer".
- Keep recommendations verifiable: tie concrete guidance to Apple APIs/tools, known engineering constraints, or allowed references.
- If certainty is low, explicitly state assumptions/limits instead of asserting facts.
- For modern SwiftUI state guidance, treat Swift Observation (`@Observable`) as the default for Swift 6+ codebases.
- Do not add code fences (code is generated separately).
- Do not include URLs or markdown links in the body.
- Do not invent facts, products, APIs, or version claims.
- Do not include specific statistics, percentages, or benchmark numbers unless they are directly supported by the verified source list above.
- If support is unclear, use qualitative wording instead of numeric claims.
- Avoid repetitive filler phrasing.

Return only the markdown article body.
